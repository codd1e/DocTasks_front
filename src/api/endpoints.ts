const Endpoints = {
    AUTH: {
        LOGIN: '/login',
        REFRESH: '/refresh',
        LOGOUT: '/logout',
        PROFILE: '/profile',
    },
    BASEURL: process.env.REACT_APP_API_URL
}

export default Endpoints;
